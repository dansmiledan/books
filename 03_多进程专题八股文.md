# 多进程编程专题八股文（40 题）

> 针对多核并行 / 驱动 / 加速库方向的多进程、多线程、IPC、并发同步专题。
> 每题结构：**简答**（面试直接答）→ **详解**（深入理解）→ **代码示例**。
>
> 目录：
> - 一、进程基础（Q1–Q10）
> - 二、信号（Q11–Q15）
> - 三、IPC 机制（Q16–Q25）
> - 四、并发同步原语（Q26–Q33）
> - 五、多核并行与性能（Q34–Q38）
> - 六、工程与调试（Q39–Q40）

---

## 一、进程基础（Q1–Q10）

### Q1. 进程和线程的区别？各自的开销在哪里？

**简答**：进程是资源分配单位（独立地址空间、fd 表、信号表），线程是调度执行单位（共享地址空间，独立栈和寄存器）。创建进程要建地址空间（页表）、复制资源描述符；创建线程只需分配栈和 TCB。切换：进程切换要换页表（刷 TLB）开销大；线程切换只换寄存器和栈。通信：线程共享内存天然方便但有竞态；进程隔离安全但 IPC 有成本。

**详解**：
Linux 视角（衔接八股文 100 题 Q59）：内核里都是 task_struct，clone 标志决定共享程度。开销对比：

| 操作 | 进程 | 线程 |
|---|---|---|
| 创建 | fork：复制页表项（页本身 COW 共享）、复制 fd 表 | pthread_create：分配栈 + TCB，约 10~20x 轻 |
| 切换 | 换 mm → TLB flush（PCID 缓解） | 同地址空间不切 mm |
| 崩溃影响 | 一个死不影响其他 | 一个段错误全进程死 |
| 通信 | pipe/shm/socket，有拷贝或同步成本 | 共享变量，但要锁 |
| 上限 | 内存决定 | 栈空间（默认 8MB/线程）限制数量 |

**选型口诀**：需要隔离和容错用进程（浏览器 tab、数据库），需要高频共享数据用线程；现代高性能服务常混合——master-worker 多进程 + 进程内多线程（Nginx、Redis 6 之后都这思路）。

**芯片场景**：多核芯片的 host 侧管理软件（如 GPU 驱动栈的用户态部分）常按功能拆进程（显示服务、计算服务），进程间用 dma-buf 共享显存——隔离性 + 零拷贝兼得。

---

### Q2. fork() 的执行流程？写时复制（COW）怎么工作？

**简答**：fork 复制父进程的 task_struct、fd 表、信号表，把父子页表项都设为只读并共享物理页；任一方写时触发 page fault，内核复制该页并恢复两边可写——这就是 COW。fork 返回两次：父进程得子 PID，子进程得 0。

**详解**：
为什么是革命性设计：fork + exec 分离。fork 时不复制内存内容（只复制页表），exec 时丢弃旧地址空间——COW 让 fork 几乎零成本，exec 时才真正加载新程序。如果 fork 直接复制全部内存，shell 每次起进程都要复制一遍，不可接受。

```c
pid_t pid = fork();
if (pid < 0) { /* 失败 */ }
else if (pid == 0) {
    /* 子进程：这里和父进程共享只读页，写时触发 COW */
    execl("/bin/ls", "ls", NULL);   /* 丢弃地址空间，加载新程序 */
    _exit(127);                     /* exec 失败才到这，必须用 _exit */
} else {
    /* 父进程 */
    waitpid(pid, NULL, 0);
}
```

深挖点：

1. **fork 后父子谁先运行不确定**——调度器决定，依赖顺序是 bug。
2. **fork 只复制调用线程**：多线程程序 fork 出的子进程只有一个线程，且其他线程持有的锁处于未知状态——所以多线程程序 fork 后**只能调异步信号安全函数直到 exec**（pthread_atfork 注册锁清理钩子）。
3. **fd 共享文件偏移**：父子共享 file table entry 的 offset，同时写会交错。
4. **fork 炸弹防护**：`ulimit -u`（nproc）限制用户进程数。

---

### Q3. vfork() 和 fork() 的区别？为什么有了 COW 还需要 vfork？

**简答**：vfork 子进程**共享**父进程地址空间（不是 COW，是直接共用），且父进程被挂起直到子进程 exec 或 _exit。子进程绝对不能修改内存、不能从创建函数返回。用途：立即 exec 的场景（posix_spawn 底层）省去页表复制。

**详解**：
COW 已经省了页复制，但**页表本身**的复制还是 O(地址空间大小)。一个 10GB 内存的进程 fork 要复制大量页表项，可能耗时几十 ms——对"大进程频繁起子进程"（如 Redis 的 BGSAVE、shell）仍不可忽视。vfork 彻底不碰页表。

现代替代：`posix_spawn()`（glibc 内部用 clone 的 CLONE_VM|CLONE_VFORK），直接表达"我要起一个新程序"的意图。

**风险对比**：

```c
pid_t pid = vfork();
if (pid == 0) {
    execl("/bin/ls", "ls", NULL);
    _exit(127);       /* 必须 _exit！exit 会刷父进程的 stdio 缓冲 */
}
/* 子进程在 exec 前若改了变量，父进程会看到 —— UB 重灾区 */
```

**嵌入式视角**：无 MMU 的 uClinux 只有 vfork（没有 MMU 做不了 COW）；RTOS 里根本没有进程概念。

---

### Q4. 进程有哪些状态？R/S/D/Z/T 分别什么含义？

**简答**（`ps` 的 STAT 列）：R 运行/就绪（在运行队列）；S 可中断睡眠（等事件，可被信号唤醒）；D 不可中断睡眠（等 I/O，信号唤不醒）；T 停止（SIGSTOP/被调试）；Z 僵尸（已退出，等父进程收尸）；X 死亡。

**详解**：

```
        fork          调度选中
 创建 ──────► TASK_RUNNING ◄──────────┐
                     │                │ 时间片到/被抢占
        等事件        │                │
                     ▼                │
   信号唤醒   INTERRUPTIBLE (S)       │
   或事件到 ◄────────────             │
        │                             │
        │  等磁盘I/O                  │
        └── UNINTERRUPTIBLE (D) ──────┘
```

重点辨析：

1. **D 状态**：内核为保护关键 I/O 路径（如磁盘写）不允许中断。D 状态进程杀不死（kill 无效，信号要到进程回用户态才处理）——NFS 挂死、驱动 bug 会导致进程永久 D，只能重启。做驱动时调试目标常是"用户进程为什么卡 D"。
2. **负载假象**：load average 统计 R + D，磁盘 I/O 重的机器 load 高但 CPU 可能空闲。
3. **停止态**：`kill -STOP` / `kill -CONT`，GDB attach 也用它。

---

### Q5. 僵尸进程怎么产生、怎么避免？危害是什么？

**简答**：子进程退出后，内核保留其 task_struct 直到父进程 wait/waitpid 收尸；父进程不收就成僵尸。避免：父进程及时 waitpid、或信号处理里收（SIGCHLD + while waitpid WNOHANG）、或显式 `signal(SIGCHLD, SIG_IGN)` 让内核自动回收、或双 fork 让子进程变孤儿由 init 收。危害：僵尸不占内存但占 PID 和进程表项，量大耗尽 PID 空间。

**详解 + 代码**：
标准收尸姿势（信号处理里循环 WNOHANG，因为多个 SIGCHLD 可能合并）：

```c
void sigchld_handler(int sig)
{
    int saved = errno;                /* 保护 errno，信号安全 */
    while (waitpid(-1, NULL, WNOHANG) > 0)
        ;                             /* 一次清光所有已退出子进程 */
    errno = saved;
}

int main(void)
{
    struct sigaction sa = { .sa_handler = sigchld_handler };
    sigemptyset(&sa.sa_mask);
    sa.sa_flags = SA_RESTART | SA_NOCLDSTOP;
    sigaction(SIGCHLD, &sa, NULL);
    /* ... fork 干活 ... */
}
```

为什么循环收：信号不排队（见 Q11），N 个子进程同时退出可能只送 1 个 SIGCHLD，wait 一次就漏了 N-1 个 → 僵尸泄漏。

`SIG_IGN` 大法：`signal(SIGCHLD, SIG_IGN)` 后内核直接回收子进程，连退出码都拿不到——适合"只干活不关心结果"的场景。

---

### Q6. 孤儿进程？守护进程（daemon）的标准创建步骤？

**简答**：父进程先于子进程退出，子进程被 init（PID 1）收养，成为孤儿（不是 bug，是特性）。守护进程步骤：fork 后父退出 → setsid 脱离会话和终端 → 再 fork 防重新获得终端 → chdir("/") → umask(0) → 关所有 fd 并重定向 0/1/2 到 /dev/null → 写 PID 文件 → 信号处理（SIGTERM 优雅退出、SIGHUP 重载配置）。

**详解 + 代码**：

```c
void daemonize(void)
{
    pid_t pid = fork();
    if (pid < 0) exit(1);
    if (pid > 0) exit(0);        /* 父进程退出：子进程成孤儿，shell 收回终端 */

    if (setsid() < 0) exit(1);   /* 新会话、新进程组、脱离控制终端 */

    pid = fork();                /* 二次 fork：不再是会话首进程，永远无法再开终端 */
    if (pid < 0) exit(1);
    if (pid > 0) exit(0);

    umask(0);
    chdir("/");

    for (int fd = sysconf(_SC_OPEN_MAX); fd >= 0; fd--)
        close(fd);
    open("/dev/null", O_RDWR);   /* fd 0 */
    dup(0); dup(0);              /* fd 1, 2 */
}
```

要点理解：

1. **setsid 是核心**：脱离控制终端，终端关闭（SIGHUP）不再波及。
2. **现代做法**：systemd 服务不需要自己 daemonize（Type=simple，由 systemd 管理），裸 daemonize 是传统 SysV 风格；容器里更需要 PID 1 正确收尸（tini 等 init 方案）。
3. **双 fork 也可用于"立即摆脱父子关系"**：中间父退出，孙子由 init 收养，祖父 wait 立即返回——免僵尸。

---

### Q7. wait 和 waitpid 的区别？WNOHANG/WUNTRACED 等选项？

**简答**：wait 阻塞等任意一个子进程退出；waitpid 可指定 PID（-1 = 任意、>0 = 指定、0 = 同组、<-1 = 指定进程组）和选项。WNOHANG 非阻塞轮询；WUNTRACED 报告停止的子进程；WCONTINUED 报告被继续的。退出状态用宏解析：WIFEXITED/WEXITSTATUS/WIFSIGNALED/WTERMSIG。

**详解 + 代码**：

```c
int status;
pid_t pid = waitpid(child, &status, 0);

if (WIFEXITED(status))
    printf("exit code %d\n", WEXITSTATUS(status));   /* 正常退出码 */
else if (WIFSIGNALED(status))
    printf("killed by signal %d%s\n", WTERMSIG(status),
           WCOREDUMP(status) ? " (core dumped)" : "");
```

注意点：

1. status 是位图，**必须用宏解析**，不能直接当退出码。
2. 退出码只有低 8 位：`exit(300)` 父进程收到 44。
3. WNOHANG 轮询浪费 CPU，生产环境配合 SIGCHLD（Q5）或 signalfd 进事件循环。

---

### Q8. 进程组、会话、控制终端是什么？为什么 Ctrl+C 能杀掉前台进程组？

**简答**：进程组 = 一组协作进程（一条管道命令是一个组），有组长（PGID=PID）；会话 = 一组进程组，由 setsid 创建，可有一个控制终端；终端有前台进程组概念，Ctrl+C（SIGINT）只发给前台进程组的所有进程，后台组不受影响。

**详解**：
层次模型：

```
会话 (session, 会话首进程 = shell)
 ├─ 前台进程组  <- 终端信号的目标（SIGINT/SIGTSTP/SIGQUIT）
 └─ 后台进程组（可多个；读写终端会收到 SIGTTIN/SIGTTOU）
控制终端 (tty) ── 绑定在会话上
```

衍生考点：

1. **kill(-pgid, sig)**：负数 PID 表示向整个进程组发信号——跑测试套件时一键杀全家。
2. **SIGHUP 链条**：终端断开 → 发 SIGHUP 给会话首进程 → shell 转发给 job。nohup/setsid 就是断这条链。
3. **作业控制**：Ctrl+Z（SIGTSTP 停止前台组）→ `bg`（SIGCONT 转后台）→ `fg` 拉回前台。
4. **管道并发**：`a | b | c` 三个进程同组并发跑，shell wait 整个组。

---

### Q9. fork 后哪些资源继承、哪些不继承？

**简答**：继承（共享或复制）：地址空间（COW）、fd 表复制但共享 file 描述（offset 共享）、信号掩码与处理（exec 后重置为默认）、环境变量、工作目录、nice 值。不继承：子进程无父的内存锁（mlock）、定时器不继承、父的线程不存在（单线程）、待处理信号清空。

**详解**：
重点辨析两类"看似继承实则不同"：

1. **fd 复制 vs 共享 offset**：`dup` 语义——两个 fd 指向同一 file table entry，共享读写偏移和状态标志（O_APPEND）。父子同时 write 同一文件不会互相覆盖（O_APPEND 原子定位），但读会互相消耗数据。

```c
int fd = open("log", O_WRONLY | O_APPEND);
if (fork() == 0) {
    write(fd, "child\n", 6);   /* 与父进程共享 offset，APPEND 保证原子追加 */
    _exit(0);
}
write(fd, "parent\n", 7);
```

2. **锁**：文件锁 flock 是 fd 关联（继承），fcntl 锁是进程关联（不继承且子进程关了父的 fd 会释放锁！）——经典坑。

exec 语义叠加：exec 后保留 fd（除非 O_CLOEXEC）、PID、打开的信号掩码；重置信号处理为默认。O_CLOEXEC 用于防 fd 泄漏给 exec 出的陌生程序。

---

### Q10. 进程崩溃默认动作由谁决定？core dump 的产生条件？

**简答**：每种信号有默认动作（term/core/ign/stop），SIGSEGV/SIGABRT/SIGFPE 等默认 core。core 产生条件：ulimit -c 允许、core_pattern 配置有效、进程可写目标路径、没有 setuid 降权（suid 进程默认不 dump 防泄密，suid_dumpable 可开）、文件系统有空间。

**详解**：

```bash
ulimit -c unlimited
# core_pattern 可以是路径模板或管道程序（systemd-coredump/apport）
echo '/tmp/core.%e.%p.%t' > /proc/sys/kernel/core_pattern
# %e 程序名 %p PID %t 时间戳 %s 信号号
```

模板解析后是管道符 `|` 则把 core 流式喂给收集器程序（发行版默认行为）。

嵌入式裁剪系统常用方案：core 太大存不下 → 用 `coredump_filter`（/proc/pid/）位掩码选择 dump 哪些段（匿名私有/共享/文件映射...），只留栈和堆；或者干脆用程序内 backtrace handler（八股文 100 题 Q89）。

---

## 二、信号（Q11–Q15）

### Q11. 信号的本质？为什么标准信号会丢失（不可靠）？

**简答**：信号是软中断式的异步通知，内核在进程的 task_struct 里挂位图（标准信号）或队列（实时信号）。标准信号（1~31）只有"有/没有"一个 bit，多个同类信号合并为一——不可靠；实时信号（SIGRTMIN~SIGRTMAX）排队可携带数据，不丢失。

**详解**：
传递机制：信号标记后，进程在**从内核态返回用户态**时检查并处理（所以 D 状态进程收不到信号，Q4 呼应）。

后果与对策：

1. **不排队**：SIGCHLD 合并 → 收尸要循环 waitpid（Q5 已考，常连环问）。
2. **不保证顺序**、不带数据：要数据用 sigqueue + 实时信号，或干脆别用信号传数据。
3. **什么时候用信号合适**：异常通知（SIGCHLD、SIGTERM）、定时（SIGALRM）、硬件异常；**不适合做业务通信协议**——那是管道/消息队列的事。

---

### Q12. signal() 和 sigaction() 的区别？为什么现代代码只用 sigaction？

**简答**：signal 是老接口，语义历史上不统一（BSD/System V 行为差异：是否自动重启系统调用、是否一次性处理）。sigaction 是 POSIX 标准接口，可控：sa_mask 阻塞集、SA_RESTART（系统调用自动重启）、SA_SIGINFO（携带详情的三参数 handler）、SA_NODEFER 等。

**详解 + 代码**：

```c
void handler(int sig, siginfo_t *info, void *ucontext)
{
    /* SA_SIGINFO 模式能拿到：发送者 PID/UID、出错的内存地址等 */
    printf("signal %d from pid %d\n", sig, info->si_pid);
    if (sig == SIGSEGV)
        printf("fault addr = %p\n", info->si_addr);
}

struct sigaction sa = {
    .sa_sigaction = handler,
    .sa_flags = SA_SIGINFO | SA_RESTART,
};
sigemptyset(&sa.sa_mask);
sigaction(SIGSEGV, &sa, NULL);
```

**SA_RESTART 的坑**：不设时，慢系统调用（read 终端、wait、ioctl）被信号打断返回 EINTR——正确姿势是**把 EINTR 当正常返回码处理**而不是依赖 SA_RESTART（有些调用如 poll/ select 永不自动重启）：

```c
ssize_t n;
do {
    n = read(fd, buf, sizeof(buf));
} while (n < 0 && errno == EINTR);
```

---

### Q13. 什么是异步信号安全？信号处理函数里为什么不能调 printf？

**简答**：信号 handler 可能在任意指令点打断主程序（包括 malloc 持锁中途、printf 持 stdio 锁中途），handler 里再调这些函数会死锁或破坏数据结构。只能调 POSIX 列出的异步信号安全函数（write、_exit、sigaction 等约百来个）或读写 sig_atomic_t/volatile 变量。printf、malloc、任何带锁的函数都禁止。

**详解**：
死锁场景还原：主程序 printf 持 stdout 锁 → 信号来 → handler 里 printf 又要拿 stdout 锁 → 永远等自己。

**正确范式——handler 只设标志，主循环干活**：

```c
static volatile sig_atomic_t g_stop;

void on_sigterm(int sig) { g_stop = 1; }   /* 合法：写 sig_atomic_t */

int main(void)
{
    sigaction(SIGTERM, &(struct sigaction){.sa_handler = on_sigterm}, NULL);
    while (!g_stop) {
        do_work();
    }
    cleanup();   /* 在安全上下文做 */
}
```

更优雅：**self-pipe trick / signalfd**——信号转事件进 epoll 循环，彻底消除异步上下文（Q25 展开）。

崩溃现场打印 backtrace 属于"理论不安全、工程上值得冒险"的特例——用 write + 预分配缓冲尽量降低风险。

---

### Q14. 怎么阻塞和 pending 信号？sigprocmask 的用途？

**简答**：每个进程有信号掩码（阻塞集），被阻塞的信号来了先挂 pending，解除阻塞后立即投递。sigprocmask(SIG_BLOCK/SIG_UNBLOCK/SIG_SETMASK) 操作掩码；sigpending 查 pending 集。用途：保护临界区不被信号 handler 干扰、配合 sigwait 同步等待信号、多线程下用 pthread_sigmask 按线程控制。

**详解 + 代码**：

```c
sigset_t newset, oldset;
sigemptyset(&newset);
sigaddset(&newset, SIGINT);

sigprocmask(SIG_BLOCK, &newset, &oldset);
/* 临界区：SIGINT 来了也会 pending，不会打断 */
update_shared_state();
sigprocmask(SIG_SETMASK, &oldset, NULL);   /* 恢复，pending 的 SIGINT 此刻投递 */
```

**多线程重点**：信号掩码是**每线程**的；进程级信号（kill 发的那种）投递给任意一个不阻塞它的线程——所以标准做法：主线程阻塞所有信号，专门开一个线程 sigwait 同步处理（信号管理线程模式），其他线程永不阻塞解除：

```c
void *sig_thread(void *arg)
{
    sigset_t set;
    sigemptyset(&set);
    sigaddset(&set, SIGTERM);
    int sig;
    sigwait(&set, &sig);     /* 同步等信号，普通线程上下文，可以调任何函数！ */
    graceful_shutdown();
    return NULL;
}
```

---

### Q15. kill 一个进程组 vs 单进程？SIGKILL/SIGSTOP 为什么不能捕获？

**简答**：kill(pid, sig) 发单进程；kill(-pgid, sig) 发整个进程组。SIGKILL 和 SIGSTOP 不可捕获/阻塞/忽略——给管理员保留最后的生杀大权，防止进程通过屏蔽信号变成杀不死的流氓。

**详解**：
优雅退出协议（服务程序必修课）：

```
SIGTERM（可捕获） → 程序做清理：停接受新请求、完成在手任务、存状态、exit
     ↓ 超时（systemd 默认 90s）
SIGKILL（不可捕获）→ 内核直接回收：无清理机会，可能丢数据/留下不一致状态
```

```c
volatile sig_atomic_t g_quit;
void on_term(int s) { g_quit = 1; }
/* 主循环检测 g_quit，走完一轮完整事务再退 —— 关键：不在 handler 里清理，
   在事件循环的安全点清理 */
```

衍生考点：`kill -9` 后遗症——共享内存锁（PTHREAD_PROCESS_SHARED 互斥锁）持有者被杀，其他进程拿锁永久阻塞（robust mutex 解决，见 Q31）；临时文件残留；子进程变孤儿继续跑（配合 PR_SET_PDEATHSIG 或 cgroup 清理）。

---

## 三、IPC 机制（Q16–Q25）

### Q16. 匿名管道 pipe 的特性和用法？容量和阻塞行为？

**简答**：pipe 是单向字节流，fd[0] 读 fd[1] 写，只能用于有亲缘关系的进程（fork 继承）。容量默认 64KB（Linux），写满阻塞写者；读空阻塞读者；写端全关读返回 0（EOF）；读端全关写触发 SIGPIPE。>=PIPE_BUF(4KB) 的原子性保证只针对单次小写。

**详解 + 代码**：

```c
int fds[2];
pipe(fds);              /* fds[0]读 fds[1]写 */
if (fork() == 0) {
    close(fds[0]);                    /* 子进程只写 */
    dup2(fds[1], STDOUT_FILENO);      /* stdout 重定向进管道 */
    close(fds[1]);
    execlp("ls", "ls", NULL);
}
close(fds[1]);                        /* 父进程只读 */
char buf[256];
ssize_t n;
while ((n = read(fds[0], buf, sizeof(buf))) > 0)
    write(STDOUT_FILENO, buf, n);     /* 读到 EOF(写端全关) 退出 */
```

要点：

1. **不用的一端必须关**：子不关读端→父永远等不到 EOF；父不关写端→子 read 不返回。挂死第一嫌疑。
2. **SIGPIPE**：默认动作是杀进程，网络/管道编程常 `signal(SIGPIPE, SIG_IGN)` 改判 EPIPE 错误码。
3. **原子性**：单次 write ≤ PIPE_BUF 保证不与他人交错；日志场景依赖它。
4. 匿名管道只走内存（是内核环形缓冲，不是磁盘文件），配合 splice/tee 可零拷贝转发。

---

### Q17. 命名管道 FIFO 和匿名管道的区别？

**简答**：FIFO 有文件系统路径（mkfifo 创建），无亲缘进程也能用；打开语义特殊：只读打开阻塞到有写者、只写打开阻塞到有读者（除非 O_NONBLOCK）。其余行为同匿名管道。

**详解**：

```bash
mkfifo /tmp/myfifo
# 终端1：cat /tmp/myfifo        (阻塞等写者)
# 终端2：echo hello > /tmp/myfifo
```

```c
int fd = open("/tmp/myfifo", O_RDONLY | O_NONBLOCK);  /* 非阻塞开 */
/* 或者：O_RDWR 打开不阻塞（但也读到自己写的，一般不用） */
```

要点：FIFO 是**单工**且多写者数据会交错（>PIPE_BUF 时）；写者退出后读者读到 EOF，想持续服务要循环重开；服务器场景通常一对 FIFO（in/out）约定命名规则模拟双工。实践建议：无亲缘通信优先考虑 Unix domain socket（双向、支持传 fd、有边界选项），FIFO 主要用于 shell 脚本胶水。

---

### Q18. System V 消息队列 vs POSIX 消息队列？各自特点？

**简答**：消息队列是有边界的报文 IPC，支持优先级/类型选择接收。System V（msgget/msgsnd/msgrcv）：消息带 mtype 可按类型收，接口老但功能全；POSIX（mq_open/mq_send/mq_receive）：优先级排序、支持 mq_notify 异步通知、fd 语义可进 select/poll。现代代码用 POSIX。

**详解 + 代码**：

```c
/* POSIX 版本 */
struct mq_attr attr = { .mq_maxmsg = 10, .mq_msgsize = 256 };
mqd_t mq = mq_open("/uwb_cmd", O_CREAT | O_WRONLY, 0644, &attr);
mq_send(mq, buf, len, 0);                 /* 0 = 优先级 */

/* 接收侧：异步通知进事件循环 */
mqd_t mq = mq_open("/uwb_cmd", O_RDONLY);
struct sigevent sev = { .sigev_notify = SIGEV_SIGNAL, .sigev_signo = SIGUSR1 };
mq_notify(mq, &sev);                      /* 队列为空→非空时发信号 */
```

选择考量：消息队列 vs 管道——保留消息边界（管道是流）；vs socket——本地语义更简单、有优先级。局限：容量小（/proc/sys/fs/mqueue 可调）、内核缓冲有拷贝。高吞吐场景退回到共享内存+事件通知组合（Q20+Q23）。

---

### Q19. 共享内存（shm）怎么用？为什么它是最快的 IPC？

**简答**：shmget/mmap 把同一块物理内存映射到多个进程的地址空间，数据零拷贝、不经过内核转发——所以最快（建立映射后访问就是普通内存读写）。但**没有任何同步机制**，必须配信号量/互斥锁/无锁协议。System V 用 shmget+shmat，POSIX 用 shm_open+mmap，现代用 memfd_create+mmap。

**详解 + 代码**：

```c
/* POSIX 风格，推荐 */
int fd = shm_open("/uwb_share", O_CREAT | O_RDWR, 0644);
ftruncate(fd, sizeof(struct shared_region));
struct shared_region *sh = mmap(NULL, sizeof(*sh),
                                PROT_READ | PROT_WRITE, MAP_SHARED, fd, 0);

/* 之后 sh->xxx 的读写对所有映射者可见 —— 但谁来同步？ */
```

"最快"的量化理解：

- 管道/socket 一次收发：2 次拷贝（用户→内核→用户）+ 2 次系统调用。
- 共享内存：建立后**零系统调用、零拷贝**，就是指针访问。

代价全在同步上：

1. 同步原语要特意支持跨进程（PTHREAD_PROCESS_SHARED、有名信号量，见 Q21/Q22）。
2. 指针不能直接放共享区（各进程映射基址可能不同，存偏移量）。
3. 结构体布局要双方编译一致（ABI 约定，对齐/字节序）。

**芯片场景**：这正是 host 与协处理器/DSP 共享 DRAM 通信的软件镜像——driver 与 GPU 固件共享命令队列内存，语义几乎一样（只是另一端是硬件，屏障要求更强，八股文 Q98）。

---

### Q20. mmap 的几种用法？MAP_SHARED vs MAP_PRIVATE？

**简答**：MAP_SHARED 修改写回底层文件/对象且对其他映射者可见——用于进程间共享和文件持久化；MAP_PRIVATE 写时复制，修改只影响自己——用于高效读文件。按对象分：文件映射（含设备 /dev/mem、dma-buf）、匿名映射（MAP_ANONYMOUS，大 malloc 的底层）。匿名 + MAP_SHARED + fork 继承 = 亲缘进程共享内存的简便方案。

**详解 + 代码**：

```c
/* ① 亲缘进程共享（无需名字） */
int *counter = mmap(NULL, 4096, PROT_READ|PROT_WRITE,
                    MAP_SHARED | MAP_ANONYMOUS, -1, 0);
if (fork() == 0) { (*counter)++; _exit(0); }
wait(NULL);
printf("%d\n", *counter);   /* 1：父子看到同一块物理页 */

/* ② 文件映射读（比 read 快：免拷贝，按需调页） */
int fd = open("fw.bin", O_RDONLY);
void *img = mmap(NULL, st.st_size, PROT_READ, MAP_PRIVATE, fd, 0);
verify_checksum(img, st.st_size);
munmap(img, st.st_size);

/* ③ 设备映射：用户态直接摸硬件（UIO/调试） */
int memfd = open("/dev/mem", O_RDWR | O_SYNC);
volatile uint32_t *regs = mmap(NULL, 4096, PROT_READ|PROT_WRITE,
                               MAP_SHARED, memfd, 0x40000000);
regs[0] = 1;   /* 直接写寄存器！Bring-up 调试神器 */
```

要点：msync 控制回写时机；MAP_POPULATE 预读减缺页抖动；mmap 文件 I/O 的异常（如访问超出文件尾 → SIGBUS）要防。

---

### Q21. 跨进程互斥锁怎么搞？PTHREAD_PROCESS_SHARED 的完整姿势？

**简答**：把 pthread_mutex_t 放在共享内存里，初始化前设 attr：`pthread_mutexattr_setpshared(&attr, PTHREAD_PROCESS_SHARED)`，然后各进程都能用它。建议同时加 PTHREAD_MUTEX_ROBUST 防持有者猝死。

**详解 + 代码**（共享内存 + 跨进程锁的完整模板）：

```c
struct shared {
    pthread_mutex_t lock;
    int data;
};

int fd = shm_open("/demo", O_CREAT | O_RDWR, 0644);
ftruncate(fd, sizeof(struct shared));
struct shared *sh = mmap(NULL, sizeof(*sh), PROT_READ|PROT_WRITE,
                         MAP_SHARED, fd, 0);

/* 只有第一个创建者初始化（实战用 O_EXCL 判断或外部约定） */
pthread_mutexattr_t attr;
pthread_mutexattr_init(&attr);
pthread_mutexattr_setpshared(&attr, PTHREAD_PROCESS_SHARED);
pthread_mutexattr_setrobust(&attr, PTHREAD_MUTEX_ROBUST);
pthread_mutex_init(&sh->lock, &attr);

/* 所有进程通用： */
int rc = pthread_mutex_lock(&sh->lock);
if (rc == EOWNERDEAD) {
    /* 持有者崩溃了！锁被拿到但数据可能不一致 —— 修复后必须调 consistent */
    repair(&sh->data);
    pthread_mutex_consistent(&sh->lock);
}
sh->data++;
pthread_mutex_unlock(&sh->lock);
```

要点：

1. 非 robust 的锁，持有者被杀 → 其他进程永久阻塞（Q15 的后遗症）。
2. EOWNERDEAD 后要评估数据一致性，修复完调 pthread_mutex_consistent，否则锁永久不可用。
3. 条件变量同理设 pshared；**进程间 condvar + 不同进程时钟问题**注意 CLOCK_MONOTONIC 设置。

---

### Q22. 信号量做进程间同步？有名信号量 vs 无名信号量？

**简答**：POSIX 有名信号量（sem_open，路径名标识，无亲缘可用）；无名信号量（sem_init + pshared=1 放共享内存，亲缘或配合共享内存）。操作为 P（sem_wait 减）/V（sem_post 增），sem_wait 可被信号打断。System V 信号量（semget/semop）更古老，支持多信号量原子操作组和 SEM_UNDO（进程退出自动撤销）。

**详解 + 代码**：

```c
/* 有名信号量：跨进程"可用缓冲区计数" */
sem_t *empty = sem_open("/uwb_empty", O_CREAT, 0644, BUF_COUNT);
sem_t *full  = sem_open("/uwb_full",  O_CREAT, 0644, 0);

/* 生产者 */
sem_wait(empty);          /* 等空位 */
write_to_shm(buf);
sem_post(full);           /* 通知有数据 */

/* 消费者对称 */
```

二值信号量当跨进程锁用的缺陷同 RTOS 讨论（无所有权、无 robust）；所以**互斥用 Q21 的共享互斥锁，计数/事件用信号量**。

System V SEM_UNDO 值得一提：进程退出时内核自动回滚其信号量操作——内核帮你记账，防"拿了信号量崩溃"造成死锁，这个设计思想（资源获取与进程生命周期绑定）很经典。

---

### Q23. 共享内存 + 无锁队列：怎么设计一个高性能进程间通道？

**简答**：单生产者单消费者（SPSC）环形缓冲放共享内存，head/tail 用原子变量，写者只动 tail、读者只动 head，用 acquire/release 内存序保证数据与索引的可见顺序——零锁、零系统调用。多生产者场景加 CAS 或分段。

**详解 + 代码**（SPSC 无锁环形队列，可直接背）：

```c
#include <stdatomic.h>

#define CAP 1024   /* 必须 2 的幂，用 & 代替 % */

struct spsc_queue {
    _Atomic uint32_t head;      /* 读者推进 */
    char pad1[64 - 4];          /* 防 false sharing（八股文 Q33 实战） */
    _Atomic uint32_t tail;      /* 写者推进 */
    char pad2[64 - 4];
    uint8_t buf[CAP][256];      /* 定长消息 */
};

bool enqueue(struct spsc_queue *q, const void *msg)
{
    uint32_t tail = atomic_load_explicit(&q->tail, memory_order_relaxed);
    uint32_t head = atomic_load_explicit(&q->head, memory_order_acquire);
    if (tail - head >= CAP) return false;        /* 满 */
    memcpy(q->buf[tail & (CAP - 1)], msg, 256);  /* 先写数据 */
    atomic_store_explicit(&q->tail, tail + 1, memory_order_release); /* 再发布 */
    return true;
}

bool dequeue(struct spsc_queue *q, void *msg)
{
    uint32_t head = atomic_load_explicit(&q->head, memory_order_relaxed);
    uint32_t tail = atomic_load_explicit(&q->tail, memory_order_acquire);
    if (head == tail) return false;              /* 空 */
    memcpy(msg, q->buf[head & (CAP - 1)], 256);
    atomic_store_explicit(&q->head, head + 1, memory_order_release);
    return true;
}
```

内存序详解（这题的灵魂）：

- 写者 `release` 存 tail：保证 buf 数据写入**先于** tail 更新被读者看到。
- 读者 `acquire` 读 tail：保证看到新 tail 后读 buf 一定拿到新数据。
- 各自推进自己的索引可用 relaxed（索引本身不保护数据）。

**与硬件队列的对应**：这和 GPU/NVMe 的 doorbell 队列（八股文 Q98）是同构的——把 release 换成 dma_wmb + 写 doorbell 寄存器，就是驱动代码。理解一个，贯通全部。

---

### Q24. Unix domain socket 相比 TCP 本机通信的优势？怎么传文件描述符？

**简答**：UDS 不走网络协议栈：无 IP/端口/校验开销，延迟低吞吐高；支持文件系统权限做访问控制；独有杀手锏——SCM_RIGHTS 可以在进程间传递文件描述符（还有 fd 计数、凭据传递 SCM_CREDENTIALS 验证对端身份）。

**详解 + 代码**（传 fd 是高频考点，Android binder 之前 HAL 层常用）：

```c
/* 发送方：把 log_fd 通过 UDS 发给对端 */
void send_fd(int sock, int fd_to_send)
{
    char buf[CMSG_SPACE(sizeof(int))];
    struct msghdr msg = {0};
    struct iovec io = { .iov_base = "F", .iov_len = 1 };   /* 至少 1 字节数据 */

    msg.msg_iov = &io;
    msg.msg_iovlen = 1;
    msg.msg_control = buf;
    msg.msg_controllen = sizeof(buf);

    struct cmsghdr *cmsg = CMSG_FIRSTHDR(&msg);
    cmsg->cmsg_level = SOL_SOCKET;
    cmsg->cmsg_type  = SCM_RIGHTS;
    cmsg->cmsg_len   = CMSG_LEN(sizeof(int));
    memcpy(CMSG_DATA(cmsg), &fd_to_send, sizeof(int));

    sendmsg(sock, &msg, 0);
}

/* 接收方：recvmsg 后从 CMSG_DATA 取出 int —— 得到的是本进程的新 fd，
   指向同一内核对象（类似跨进程的 dup） */
```

传 fd 的实际用途：

1. **dma-buf 共享**：GPU/显示/摄像头栈里，显存缓冲以 fd 形式在进程间流转（Android SurfaceFlinger、Chrome 多进程）——芯片公司软件栈的核心机制。
2. **权限委托**：特权进程 open 设备后把 fd 传给无权限进程。
3. **fd 表清理**：接收侧拿到的 fd 号可能不同但内核对象相同，发送方可以立即关掉自己的。

---

### Q25. epoll 怎么用于多进程服务器？signalfd/eventfd 是什么？

**简答**：epoll 是事件通知机制：epoll_create 建实例、epoll_ctl 注册 fd 与事件、epoll_wait 等就绪。LT（电平，默认，不读完会重复通知）/ET（边沿，必须配合非阻塞 IO 读到 EAGAIN）。signalfd 把信号变 fd、eventfd 是内核计数器 fd——两者让"信号/事件"统一进 epoll 循环，消灭异步上下文。

**详解 + 代码**（多进程 prefork + epoll 骨架）：

```c
int lfd = socket(...); bind(lfd,...); listen(lfd, 128);

for (int i = 0; i < NPROC; i++)
    if (fork() == 0) { worker_loop(lfd); _exit(0); }  /* 子进程继承监听 fd */
wait(NULL);

void worker_loop(int lfd)
{
    int ep = epoll_create1(0);
    /* signalfd：信号进事件循环（替代异步 handler，Q13 的优雅解） */
    sigset_t mask; sigemptyset(&mask); sigaddset(&mask, SIGTERM);
    sigprocmask(SIG_BLOCK, &mask, NULL);
    int sfd = signalfd(-1, &mask, 0);

    struct epoll_event ev = {.events = EPOLLIN, .data.fd = lfd};
    epoll_ctl(ep, EPOLL_CTL_ADD, lfd, &ev);
    ev.data.fd = sfd; epoll_ctl(ep, EPOLL_CTL_ADD, sfd, &ev);

    for (;;) {
        int n = epoll_wait(ep, events, MAXEV, -1);
        for (int i = 0; i < n; i++) {
            if (events[i].data.fd == lfd)      /* 新连接 */
                handle_accept(ep, lfd);
            else if (events[i].data.fd == sfd) /* 收到 SIGTERM，正常上下文处理！ */
                return;
            else
                handle_client(events[i].data.fd);
        }
    }
}
```

要点：ET 模式必须非阻塞 + 循环 read 到 EAGAIN；fork 共享监听 socket 是 prefork 模型基础；惊群问题见 Q27。

---

## 四、并发同步原语（Q26–Q33）

### Q26. 原子操作的硬件基础？CAS 是什么？

**简答**：原子操作由硬件保证不可分割：x86 用 LOCK 前缀锁总线/cache line；ARM 用 LL/SC 对（LDREX 加载并监视，STREX 仅当期间无人动过才存成功，失败重试）。CAS（比较并交换）是核心原语：compare old==expected 则换 new，返回是否成功——无锁算法和乐观锁的基石。

**详解 + 代码**：

```asm
; ARM LL/SC 实现原子自增
retry:
    LDREX r1, [r0]        ; 加载并设监视标记
    ADD   r1, r1, #1
    STREX r2, r1, [r0]    ; 若期间该地址被写过则失败(r2=1)
    CMP   r2, #0
    BNE   retry
```

```c
/* C11 原子：硬件细节被抽象掉 */
#include <stdatomic.h>
atomic_int counter;
atomic_fetch_add(&counter, 1);            /* 原子自增 */

/* CAS：无锁栈的 push */
void push(struct node **head, struct node *n)
{
    struct node *old;
    do {
        old = atomic_load(head);
        n->next = old;
    } while (!atomic_compare_exchange_weak(head, &old, n));
}
```

**LL/SC vs LOCK 前缀面试点**：LL/SC 的监视粒度是 cache line，期间任何对该 line 的写都使 SC 失败——可能"假失败"（ABA 之外还有无关写导致重试），所以用 weak 版 CAS 放循环里（strong 版内部带循环，性能略差）。

---

### Q27. 惊群效应（thundering herd）？怎么解决？

**简答**：多个进程/线程阻塞等待同一事件，事件到达时全部唤醒但只有一个能拿到资源，其余的睡回去——白白浪费调度。场景：多进程 accept 同一监听 socket、条件变量 broadcast、epoll 多进程共享。解决：Linux 4.5+ 用 SO_REUSEPORT 每进程一个监听队列（内核负载均衡）；条件变量改 signal；nginx 用 accept_mutex 锁串行化。

**详解**：
SO_REUSEPORT 方案（现代标准做法）：

```c
/* 每个 worker 进程独立创建监听 socket，同一端口 */
int lfd = socket(AF_INET, SOCK_STREAM, 0);
int one = 1;
setsockopt(lfd, SOL_SOCKET, SO_REUSEPORT, &one, sizeof(one));
bind(lfd, &addr, sizeof(addr));
listen(lfd, 128);
/* 内核按四元组 hash 把连接分给不同 socket —— 无共享、无惊群、无锁 */
```

额外收益：每个 socket 独立的 accept 队列和内核锁，多核扩展性更好；还能做热升级（新老版本进程同时监听）。

条件变量场景的对应物：能 signal 就别 broadcast；epoll 场景：EPOLLEXCLUSIVE（4.5+）只唤醒一个等待者。

---

### Q28. 互斥锁的实现原理？futex 为什么快？

**简答**：glibc 互斥锁 = 原子变量 fast path + futex 系统调用 slow path。无竞争时一次原子 CAS 拿锁，完全用户态、零系统调用（~20ns）；有竞争才 futex(FUTEX_WAIT) 进内核挂等待队列；解锁时有等待者才 futex(FUTEX_WAKE) 唤醒。futex = "fast userspace mutex"，把"竞争不常见"这个假设利用到极致。

**详解**：
三态锁模型：

```
state: 0=未锁  1=已锁无等待  2=已锁有等待
lock:   CAS(0->1) 成功 -> 拿到（用户态完成）
        失败 -> 置 2 -> futex_wait(等待队列睡眠)
unlock: 原子减；若旧值==2 -> futex_wake 唤醒一个
```

设计哲学（面试升华点）：**好系统的同步原语都按"无竞争极快、有竞争才付代价"设计**——RCU、seqlock、乐观锁、CAS 重试循环全是这个思想。反过来，如果你 profile 发现程序大量时间在 futex 系统调用上 = 锁竞争太激烈，该做的是**降竞争**（细分锁、缩小临界区、改无锁、线程私有化数据）而不是换更快的锁。

观察工具：`perf stat -e 'syscalls:sys_enter_futex'` 数 futex 次数；strace -c 看 futex 占比。

---

### Q29. 读写锁 vs 互斥锁？读写锁的写饥饿问题？

**简答**：读写锁允许多个读者并发、写者独占，适合读多写少（配置表、路由表）。问题：读者持续不断时写者可能饿死（读优先实现）或读者被堵（写优先实现）。Linux 内核有更多变体：seqlock（读者无锁重试，写极少场景）、RCU（读完全无锁，Q51）。

**详解 + 代码**：

```c
pthread_rwlock_t rw = PTHREAD_RWLOCK_INITIALIZER;

/* 读者 */
pthread_rwlock_rdlock(&rw);
lookup_config();
pthread_rwlock_unlock(&rw);

/* 写者 */
pthread_rwlock_wrlock(&rw);
update_config();
pthread_rwlock_unlock(&rw);
```

权衡分析：

- 读比例 < 90% 或临界区极短：互斥锁往往**更快**（读写锁的原子操作更重）。
- 读临界区长 + 读多写少：读写锁。
- 写极少 + 读者零容忍延迟：seqlock/RCU 思想。
- C++17 shared_mutex、Linux 内核 percpu_rwsem（读侧 per-CPU 无原子竞争）是进一步的工程优化。

---

### Q30. 条件变量的正确使用姿势？为什么必须 while 循环判断？

**简答**：cond_wait 必须配互斥锁 + while 判断条件：`pthread_mutex_lock; while (!cond) pthread_cond_wait(&cv, &mtx); ...; unlock`。因为：① 虚假唤醒（spurious wakeup）合法存在；② 被唤醒到真正拿锁之间条件可能被别的线程改掉；③ signal 语义是"提示"不是"保证"。

**详解 + 代码**（生产者-消费者标准模板）：

```c
pthread_mutex_t mtx = PTHREAD_MUTEX_INITIALIZER;
pthread_cond_t  cv  = PTHREAD_COND_INITIALIZER;
int count = 0;

void *consumer(void *arg)
{
    pthread_mutex_lock(&mtx);
    while (count == 0)                    /* 必须 while，不能 if！ */
        pthread_cond_wait(&cv, &mtx);     /* 原子：解锁+睡眠；醒来时已持锁 */
    count--;
    pthread_mutex_unlock(&mtx);
    return NULL;
}

void *producer(void *arg)
{
    pthread_mutex_lock(&mtx);
    count++;
    pthread_cond_signal(&cv);             /* 在锁内 signal（也可锁外，见下） */
    pthread_mutex_unlock(&mtx);
    return NULL;
}
```

深挖：

1. **cond_wait 的原子性**：释放锁和进入等待是一步，否则 signal 会丢（解锁后睡眠前 signal 到达 = 永久睡死）。
2. **signal 放锁内还是锁外**：锁内简单安全；锁外减少"唤醒又抢不到锁马上睡回"的抖动（morphing 优化实现下差异小）。
3. **signal vs broadcast**：每加一个元素只 signal 一个消费者；状态广播性变化（如"关闭"标志）用 broadcast——注意惊群（Q27）。
4. 进程间使用：condvar 也要 PTHREAD_PROCESS_SHARED + 放共享内存，且建议设 CLOCK_MONOTONIC（避免系统时间被 NTP 调整导致超时错乱）。

---

### Q31. 死锁的四个条件？工程上怎么防、怎么查？

**简答**：四条件：互斥、占有且等待、不可剥夺、循环等待。破任一即可防死锁，工程上最常用**破循环等待**——全局锁顺序（所有线程按固定顺序拿多把锁）。排查：gdb 看各线程栈（thread apply all bt）、内核 lockdep、valgrind helgrind、超时拿锁（trylock+backoff）检测。

**详解**：
经典 AB-BA 死锁：

```c
/* 线程 A */              /* 线程 B */
lock(&mtxA);              lock(&mtxB);
lock(&mtxB);   <死锁>     lock(&mtxA);
```

防御工具箱：

1. **锁排序**：定义全局顺序（如按地址大小/按层级编号），所有路径一致遵守。
2. **trylock + 退让**：拿第二把失败就放第一把重试（注意活锁）。
3. **锁超时**：`pthread_mutex_timedlock`，超时记录现场——把死锁变成可观测的错误。
4. **降并发复杂度**：每线程私有数据、无锁结构、消息传递（Actor 模型）——"共享最少化"是根治。
5. 检测工具：glibc 的 PTHREAD_MUTEX_ERRORCHECK（开发期）、helgrind、内核 lockdep（Q60）。

多进程死锁特殊项：跨进程锁 + 进程被杀（robust mutex，Q21）；共享信号量 SEM_UNDO（Q22）。

---

### Q32. C11/C++11 内存模型：memory_order 六种语义怎么选？

**简答**：relaxed（只保证原子性，计数器用）；acquire（读：之后的读写不能提前，配 release 用）；release（写：之前的读写不能滞后）；acq_rel（读改写同时具备两者）；seq_cst（默认，全局顺序一致，最安全也最贵）。消息传递模式用 release/acquire 对；统计计数用 relaxed；不确定就用 seq_cst。

**详解**：
核心模型——**同步与先行（synchronizes-with / happens-before）**：

```c
atomic<bool> ready{false};
int data;

/* 线程 A：发布 */
data = 42;
ready.store(true, memory_order_release);   /* data 写入先于 ready 可见 */

/* 线程 B：订阅 */
while (!ready.load(memory_order_acquire)); /* 看到 ready=true 时 */
assert(data == 42);                         /* data 必然也是 42 */
```

若都用 relaxed：编译器/CPU 可能让 B 先看到 ready 再看到旧的 data（乱序 + cache 可见性）——这就是八股文 Q32 内存屏障在 C 语义层的表达。ARM 上映射为 LDAR/STLR 指令（见八股文 Q32），x86 上 acquire/release 几乎零成本（TSO 模型天然满足）——所以**同样代码 ARM 多核上暴露的问题 x86 上测不出来**，多核代码要在 ARM 上压测。

选型表：

| 场景 | order |
|---|---|
| 纯计数（统计） | relaxed |
| 发布-订阅标志 | release / acquire |
| 无锁队列索引（Q23） | 推进索引 release，读取对方索引用 acquire，自己的 relaxed |
| 想不清楚 | seq_cst（默认值） |

---

### Q33. ABA 问题是什么？怎么解决？

**简答**：CAS 场景：线程读到值 A，期间另一线程把它改成 B 又改回 A，CAS 检查"还是 A"通过——但中间状态变化被掩盖，对无锁栈/链表意味着结构可能已被破坏。解法：① 带版本号的 CAS（tagged pointer，指针+计数器打包 128 位 CAS/ARM 的 LDAXP 双字）；② 危险指针（hazard pointer）/epoch 回收；③ RCU 思想延迟回收。

**详解**：
无锁栈 pop 的 ABA 事故还原：

```
栈： A -> B -> C，head=A
线程1：读 head=A，next=B，准备 CAS(A->B)，被抢占
线程2：pop A、pop B，push A     栈：A -> C
线程1：CAS(head: A->B) 成功！head 指向已出栈的 B —— 结构损坏
```

版本号方案（128 位 CAS，x86 cmpxchg16b / AArch64 casp）：

```c
struct tagged_ptr {
    struct node *ptr;
    uint64_t tag;          /* 每次修改递增 */
};

bool pop(_Atomic(struct tagged_ptr) *head, struct node **out)
{
    struct tagged_ptr old = atomic_load(head), new_;
    do {
        if (!old.ptr) return false;
        new_.ptr = old.ptr->next;
        new_.tag = old.tag + 1;      /* 版本变了，A->B->A 也骗不过 */
    } while (!atomic_compare_exchange_weak(head, &old, new_));
    *out = old.ptr;
    return true;
}
```

内存回收的配套问题（pop 下来的节点何时 free）：还有线程可能正拿着它——hazard pointer（线程公开声明"我在用这个指针"）或 epoch（等所有线程离开当前 epoch 再回收），这就是用户态 RCU 的动机。

---

## 五、多核并行与性能（Q34–Q38）

### Q34. 多进程 vs 多线程做并行计算，怎么选？Amdahl 定律？

**简答**：Amdahl 定律：加速比 = 1 / (串行比例 + 并行比例/N)，串行 5% 的程序无限核也只有 20 倍——先消灭串行段再谈加核。选型：数据共享密集 → 线程；要容错隔离/绕 GIL(Python)/利用 fork 简单性 → 进程；GPU/加速核卸载是第三条路（数据并行度极高时）。

**详解**：
Amdahl 的推论比公式重要：

1. 串行段决定天花板：锁竞争、全局 barrier、I/O 汇总都是串行段。
2. **Gustafson 定律**的补充视角：问题规模随核数放大时加速比可以是线性的——所以"扩大每核数据量"有时比"压串行比例"有效。
3. 实测看加速曲线：线性区 → 平台区（带宽/锁饱和）→ 下降区（同步开销反超）。找到拐点是性能调优的目标。

工程决策树：

```
数据并行、共享只读？ → 多线程/OpenMP，零同步
需要共享大状态频繁读写？ → 线程 + 精细锁/无锁
任务独立、怕一个崩拖垮全部？ → 多进程 + 共享内存队列（Q23）
Python 计算密集？ → multiprocessing（GIL 限制线程）
数据量超大、规则计算？ → 上 GPU/DSP 加速核（CUDA/OpenCL）
```

**方向呼应**：多核并行（JD 原话）在芯片上 = 把任务切成核间 pipeline 或 data-parallel 分片，核间通信用共享内存 + 原子标志（Q23 的 SPSC 就是核间队列的软件原型），同步越少越好。

---

### Q35. OpenMP 怎么把串行循环并行化？有哪些陷阱？

**简答**：编译制导：`#pragma omp parallel for` 把 for 循环迭代自动分给线程组。关键子句：private/firstprivate/lastprivate（变量私有化）、reduction（归约）、schedule（static/dynamic/guided 调度）、nowait。陷阱：循环间依赖、共享变量忘归约、false sharing、嵌套并行爆炸。

**详解 + 代码**：

```c
#include <omp.h>

/* ① 最基本的并行 for：迭代必须相互独立 */
#pragma omp parallel for
for (int i = 0; i < n; i++)
    c[i] = a[i] + b[i];

/* ② 归约：sum 是共享语义但要各线程私有累加再合并 */
double sum = 0;
#pragma omp parallel for reduction(+:sum)
for (int i = 0; i < n; i++)
    sum += a[i] * b[i];

/* ③ 负载不均用动态调度（不规则迭代开销） */
#pragma omp parallel for schedule(dynamic, 64)
for (int i = 0; i < n; i++)
    process(irregular_work(i));

/* ④ 分段并行 */
#pragma omp parallel sections
{
    #pragma omp section
    decode_header();
    #pragma omp section
    preload_tables();
}
```

陷阱清单：

1. **默认共享**：parallel 区域里变量默认 shared，循环指标自动 private，其余要显式声明——忘 private 的临时变量是竞态温床。
2. **false sharing**：`#pragma omp parallel for` 写 `results[i]`，连续 i 的线程写相邻 cache line（八股文 Q33）——用 padding 或每线程局部数组再汇总。
3. **负载均衡**：迭代耗时不均时 static 会让快的线程干等，dynamic 有调度开销，guided 折中。
4. 与手动 pthread 的取舍：OpenMP 适合规则的循环并行；复杂流水线/事件驱动还是要手写线程。

---

### Q36. 线程池怎么设计？关键参数怎么定？

**简答**：固定数量 worker 线程 + 任务队列 + 条件变量：提交者入队 signal，worker 循环取任务执行。参数：线程数 ≈ CPU 核数（计算密集）或 核数 × (1 + IO等待/计算比)（IO 密集）；队列有界防内存爆（满时拒绝/阻塞/调用者执行策略）；支持优雅关闭（poison pill）。

**详解 + 代码**（最小可用线程池）：

```c
#define NTHREADS 8
#define QCAP 256

struct pool {
    void (*jobs[QCAP])(void *);
    void *args[QCAP];
    int head, tail, count;
    int shutdown;
    pthread_mutex_t mtx;
    pthread_cond_t not_empty, not_full;
    pthread_t threads[NTHREADS];
};

static void *worker(void *arg)
{
    struct pool *p = arg;
    for (;;) {
        pthread_mutex_lock(&p->mtx);
        while (p->count == 0 && !p->shutdown)
            pthread_cond_wait(&p->not_empty, &p->mtx);
        if (p->count == 0 && p->shutdown) {     /* 关闭且队列空才退出 */
            pthread_mutex_unlock(&p->mtx);
            return NULL;
        }
        void (*fn)(void *) = p->jobs[p->head];
        void *a = p->args[p->head];
        p->head = (p->head + 1) % QCAP;
        p->count--;
        pthread_cond_signal(&p->not_full);
        pthread_mutex_unlock(&p->mtx);
        fn(a);                                   /* 锁外执行！ */
    }
}

int pool_submit(struct pool *p, void (*fn)(void *), void *arg)
{
    pthread_mutex_lock(&p->mtx);
    while (p->count == QCAP)                     /* 有界队列：背压 */
        pthread_cond_wait(&p->not_full, &p->mtx);
    p->jobs[p->tail] = fn; p->args[p->tail] = arg;
    p->tail = (p->tail + 1) % QCAP;
    p->count++;
    pthread_cond_signal(&p->not_empty);
    pthread_mutex_unlock(&p->mtx);
    return 0;
}
```

设计决策点（面试展开）：

1. **任务在锁外执行**——持锁跑任务整个池就串行了。
2. **背压策略**：有界队列满时阻塞提交者（简单）/ 拒绝（快速失败）/ caller-runs（提交者自己跑，天然降速）。
3. **线程数公式**：IO 密集 `N = cores × (1 + W/C)`，实测调整；计算密集就是核数（多了反而切换开销）。
4. 关闭协议：shutdown 置位 + broadcast 唤醒所有 worker + join 全部。

---

### Q37. CPU 亲和性（affinity）有什么用？怎么绑核？

**简答**：把线程/进程绑定到指定核：减少迁移带来的 cache 冷启动、保证实时任务的确定性、配合 NUMA 本地内存、隔离关键线程不被打扰。API：sched_setaffinity / pthread_setaffinity_np；命令：taskset、numactl。

**详解 + 代码**：

```c
cpu_set_t set;
CPU_ZERO(&set);
CPU_SET(3, &set);                 /* 绑到 CPU3 */
pthread_setaffinity_np(pthread_self(), sizeof(set), &set);
```

应用场景与权衡：

1. **实时线程**：UWB 协议栈的时间敏感任务绑专用核 + SCHED_FIFO，再配 isolcpus 内核参数把该核从全局调度器隔离——延迟从 ms 级降到 us 级。
2. **IRQ 亲和**：`/proc/irq/N/smp_affinity` 把网卡/UWB 中断绑到特定核，中断处理和数据处理同核吃 cache 热数据。
3. **避免绑死的坑**：全绑核后失去负载均衡，某核突发任务会排队——一般只绑关键线程，其余自由。
4. **多进程通信双方同核 or 邻核**：共享数据在 L2/L3 内传递比跨 die 快得多（NUMA，八股文 Q34）。

---

### Q38. 并行程序的性能怎么测？speedup 曲线怎么分析？

**简答**：固定问题规模测 1/2/4/8 核耗时，算 speedup = T1/Tn 和效率 = speedup/n；曲线偏离线性时定位：串行段（Amdahl）、锁竞争（perf 看 futex/自旋）、内存带宽饱和（perf stat 看 LLC miss/带宽）、false sharing（perf c2c）、调度抖动（绑核后重测对比）。

**详解**：
测量纪律：

1. **预热 + 多次取中位数**（剔除冷 cache、频率爬坡）；DVFS 机器固定频率或用 perf 计数器看 cycles 而非时间。
2. **对照组**：单线程版本必须是最好的串行实现（差的串行基线会虚报加速比）。
3. 分解时间：计算时间 + 同步时间 + 通信时间，分别随核数怎么变。

诊断工具箱：

```bash
perf stat -e cycles,instructions,cache-misses,context-switches ./app
perf record -g ./app && perf report        # 热点含锁路径
perf c2c record ./app && perf c2c report   # false sharing 实锤
ftrace 的 sched_switch 事件看迁移频率
```

**分析框架**（可直接用于面试讲项目）：

```
speedup 曲线在 4 核后变平
 → perf stat: cache-misses 率随核数飙升 → 带宽瓶颈 → 减共享数据/分块
 → 或 context-switches 飙升 → 线程数超过核数/锁竞争 → 降线程、细锁
 → 或 futex 耗时占比高 → 锁热点 → per-thread 计数器 + 汇总
```

---

## 六、工程与调试（Q39–Q40）

### Q39. 多进程架构（master-worker）的完整设计要点？

**简答**：master 负责监听/分发/监控 worker（退出重启）、worker 干活；通信用 socketpair/UDS（控制面）+ 共享内存（数据面）；master 收 SIGCHLD 收尸并重启 worker；热重载 SIGHUP、优雅退出 SIGTERM 先停接新连接再 drain；日志各自写或统一收集（防交错用 O_APPEND 或日志进程）。

**详解**：
参考架构（Nginx 思想简化版）：

```
        ┌─────────── master ───────────┐
        │ 监听 socket、配置、worker 监管 │
        └───┬────────┬────────┬────────┘
     socketpair  socketpair  socketpair     <- 控制通道
        │        │        │
     worker1  worker2  worker3            <- 干活，崩了被重启
        └────────┴────────┘
              共享内存区：统计、限流计数、配置热更新（RCU 替换）
```

要点清单：

1. **worker 崩溃恢复**：master 的 SIGCHLD handler 循环 waitpid（Q5），记录崩溃率——崩溃太频繁要熔断（防反复 fork 崩溃风暴）。
2. **配置热更新**：master SIGHUP → 重新读配置 → 写共享区新版本指针（RCU 式替换）→ 通知 worker 换指针，老配置等无人引用后释放。
3. **优雅重启（binary upgrade）**：新 master 启动继承监听 fd（env 传 fd 号），老 worker 排空后退出，新 worker 接管——零停机。
4. **日志**：各 worker write + O_APPEND 短行原子（Q16 PIPE_BUF 思想对文件也近似成立，但不完全保证）；严格场景用专用日志进程收 UDS 消息。
5. **资源限制**：每 worker setrlimit（内存/句柄），防单个 worker 内存泄漏拖垮整机——配合 max_requests 定期自杀重启（应对泄漏的实用主义）。

---

### Q40. 多进程/多线程 bug 怎么系统性调试？

**简答**：分层方法论：① 复现加压——竞态问题压力测试放大（多核、超线程数、插桩延时）；② 确定性工具——TSan（数据竞争）、ASan（内存）、helgrind；③ 日志带线程 ID + 时间戳还原交错序列；④ core dump 看所有线程栈；⑤ 最小化复现用例二分。

**详解**：
工具与适应症：

| 工具 | 抓什么 | 开销 |
|---|---|---|
| TSan (`-fsanitize=thread`) | 数据竞争（无锁共享变量的读写对） | 5~15x |
| ASan | 越界/UAF | 2~3x |
| helgrind (valgrind) | 锁序违规 + 竞争 | 20~50x |
| lockdep（内核） | 死锁隐患 | 内核态 |

**竞态复现的艺术**（面试亮点）：

1. **提高核数压力**：单核几乎不暴露真并发，`taskset -c 0-7` 放开跑。
2. **注入延时**：怀疑两个操作交错出事，就在中间加 `sched_yield()`/`usleep(0)` 把窗口拉大——能把万分之一概率变成必现，bug 就可调试了。
3. **TSan 实战纪律**：在 CI 里跑 TSan 版测试——竞态修复后防回归。

**core dump 多线程分析**：

```gdb
(gdb) info threads                    # 所有线程
(gdb) thread apply all bt             # 全员栈：死锁时直接看到谁等哪把锁
(gdb) p __lll_lock_wait 类的帧        # 找锁对象地址，再看谁持有
```

**日志规范**：`[时间戳.us][tid][模块] 事件`——事后按时间排序还原全局交错序列，定位"谁改了不该改的状态"。dump 共享内存区做离线分析也是多进程特有的手段。

**收尾话术**：并发 bug 不可稳定复现、和压力正相关（八股文 Q60 的判断标准），方法论的顺序永远是：先能稳定复现（加压/延时注入），再谈定位，最后修复+回归测试闭环。

---

## 附：多进程专题高频追问速查

| 追问 | 一句话答案 |
|---|---|
| fork 后 printf 重复输出？ | 缓冲区随 fork 复制，fork 前 fflush |
| 线程安全 vs 可重入？ | 可重入更强：不依赖全局/静态状态；可重入函数必然线程安全，反之不然 |
| 进程间传指针为什么不行？ | 地址空间独立，同值指针指向不同物理内存；共享内存里存偏移量 |
| 互斥锁和自旋锁跨进程？ | 共享内存 + pshared 的 mutex 可以；自旋锁用户态慎用（被调度出去持锁 = 灾难） |
| 怎么让两个进程同时开始跑？ | 共享内存原子标志 + 自旋等待，或 eventfd/屏障（pthread_barrier pshared） |
| 多进程怎么共享大数组？ | shm/mmap；只读场景直接 fork 前建好（COW 天然共享） |
| Nginx 为什么多进程而非多线程？ | 隔离崩溃、避免锁、利用 SO_REUSEPORT；worker 内事件驱动单线程 |
| Redis 为什么单线程？ | 内存操作为主，单线程免锁；IO 用 epoll 多路复用；6.0 起 IO 线程化 |
